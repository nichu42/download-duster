name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Set version from tag
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          sed -i "s/\"version\": \"[^\"]*\"/\"version\": \"$VERSION\"/" manifest.json
          sed -i "s/\"version\": \"[^\"]*\"/\"version\": \"$VERSION\"/" manifest.firefox.json

      - name: Build packages
        run: bash build.sh

      - name: Remove unsigned XPI
        run: rm -f dist/download-duster-firefox.xpi

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Sign Firefox extension
        env:
          AMO_JWT_ISSUER: ${{ secrets.AMO_JWT_ISSUER }}
          AMO_JWT_SECRET: ${{ secrets.AMO_JWT_SECRET }}
        run: |
          set +e
          npx --yes web-ext sign \
            --source-dir dist/firefox \
            --artifacts-dir dist \
            --api-key "$AMO_JWT_ISSUER" \
            --api-secret "$AMO_JWT_SECRET" \
            --channel listed \
            --timeout 900000 2>&1 | tee /tmp/web-ext-out.txt
          EXIT=${PIPESTATUS[0]}
          set -e

          if [ $EXIT -ne 0 ]; then
            if grep -q "already exists" /tmp/web-ext-out.txt; then
              echo "Version already signed on AMO — downloading existing XPI..."
              python3 .github/scripts/amo_download.py
            elif grep -q "approval" /tmp/web-ext-out.txt; then
              echo "Extension is pending AMO review — downloading XPI via API..."
              python3 .github/scripts/amo_download.py
            else
              echo "web-ext sign failed with: $(cat /tmp/web-ext-out.txt)"
              echo "Attempting to download existing XPI from AMO as fallback..."
              if ! python3 .github/scripts/amo_download.py; then
                exit $EXIT
              fi
            fi
          fi

      - name: Upload & Publish to Chrome Web Store
        env:
          CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}
          EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          PUBLISHER_ID: ${{ secrets.CHROME_PUBLISHER_ID }}
        run: |
          npx --yes --package=chrome-webstore-upload-cli chrome-webstore-upload \
            --source dist/download-duster-chrome.zip \
            --extension-id "$EXTENSION_ID" \
            --publisher-id "$PUBLISHER_ID"

      - name: Find signed XPI
        run: |
          SIGNED_XPI=$(find dist -maxdepth 1 -name "*.xpi" | head -1)
          if [ -z "$SIGNED_XPI" ]; then
            echo "ERROR: No signed XPI found in dist/"
            exit 1
          fi
          echo "Found signed XPI: $SIGNED_XPI"
          echo "SIGNED_XPI=$SIGNED_XPI" >> "$GITHUB_ENV"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/download-duster-chrome.zip
            ${{ env.SIGNED_XPI }}
          prerelease: ${{ contains(github.ref_name, '-') }}
